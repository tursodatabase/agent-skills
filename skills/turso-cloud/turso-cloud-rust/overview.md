# Rust

| Use case | Package | Install |
|----------|---------|---------|
| Remote-only (libsql protocol) | `libsql` | `cargo add libsql` |

The `libsql` crate connects directly to a remote Turso Cloud database over the libsql protocol.

## Docs

| Topic | URL |
|-------|-----|
| Rust SDK quickstart | `https://docs.turso.tech/sdk/rust/quickstart.md` |
